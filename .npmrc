# Exact versions rather than ranges. A published library should be able to
# reproduce a build from the lockfile alone, and caret ranges in devDependencies
# are a recurring source of "works locally, fails in CI".
save-exact=true

# Refuse to install if the local Node version does not satisfy the engines
# field, rather than failing later with a confusing runtime error.
engine-strict=true
